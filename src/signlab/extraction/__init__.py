"""Version-pinned, synthetic-eligibility-gated landmark extraction services.

Public APIs live in the focused ``batch``, ``parquet``, ``resources``, ``runtime``,
and ``tracking`` modules so importing this package never initializes heavy adapters.
"""
