# Examples

Examples are small, executable demonstrations built from synthetic, explicitly
consented, or separately licensed inputs. They may call public `signlab` APIs, but
production logic belongs in `src/signlab` and reusable test data belongs under
`tests/fixtures/public` with provenance.
